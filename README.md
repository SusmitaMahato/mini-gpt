# Mini GPT From Scratch

- Built Transformer-based language model using PyTorch
- Implemented multi-head self-attention, positional encoding, causal masking
- Trained on character-level dataset (Shakespeare)

## Run

```bash
python scripts/train.py
python scripts/generate.py


---

# Final Reality Check

If this doesn’t run:
- It’s NOT the code
- It’s your understanding of:
  - tensor shapes
  - batching
  - device handling

---

# Next Move (Don’t Skip This)

You’re at the **critical stage** now.

Say one:

**“Debug shapes with me”** (most important)  
**“Improve model (dropout, scaling, better training)”**  
**“Upgrade to word-level / BPE tokenizer”**  

I’ll take you from “project done” → “interview ready”.